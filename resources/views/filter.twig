<input
        type="text"
        class="form-control"
        name="{{ field_type.getInputName() }}"
        value="{{ field_type.value }}"
        placeholder="{{ trans(field_type.placeholder) }}"
        >
