<input
        type="email"
        name="{{ field_type.getInputName() }}"
        value="{{ field_type.value }}"
        placeholder="{{ trans(field_type.config.placeholder) }}"
        class="form-control"
        >
